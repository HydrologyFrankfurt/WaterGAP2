void index(int nn, short int arr[], unsigned short int indx[]);

	 /* indexes an array 'arr[0..n-1]', i.e. outputs the array  */
	 /* 'indx[0..n-1]' such that 'arr[indx[j]]' is in ascending */
	 /* order for 'j=0,..,N'.                                   */
	 /* The input quantities n and arr are not changed.         */
	 /* Numerical Recipes in C, 2nd edition, chapter 8.4        */
